# Password_note_manager

A password and notes management app using MERN stack. Also Redux is used along with react as state management tool. JWT tokens are implemented to authorise users to access data.